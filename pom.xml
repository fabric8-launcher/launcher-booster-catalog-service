<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
   <modelVersion>4.0.0</modelVersion>
   <parent>
      <groupId>org.jboss</groupId>
      <artifactId>jboss-parent</artifactId>
      <version>24</version>
   </parent>
   <groupId>io.openshift</groupId>
   <artifactId>booster-catalog-service</artifactId>
   <version>4-SNAPSHOT</version>
   <licenses>
      <license>
         <name>Apache License, Version 2.0</name>
         <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
         <distribution>repo</distribution>
         <comments>A business-friendly OSS license</comments>
      </license>
   </licenses>
   <build>
      <plugins>
         <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <executions>
               <execution>
                  <id>create-openshift-online-free-catalog</id>
                  <phase>prepare-package</phase>
                  <goals>
                     <goal>java</goal>
                  </goals>
                  <configuration>
                     <mainClass>io.openshift.booster.BoosterIndexer</mainClass>
                     <arguments>
                        <argument>${catalog.repository}</argument>
                        <argument>openshift-online-free</argument>
                        <argument>${project.build.directory}/booster-catalog-openshift-online-free</argument>
                        <argument>${project.build.outputDirectory}/booster-catalog-openshift-online-free.zip</argument>
                     </arguments>
                  </configuration>
               </execution>
               <execution>
                  <id>create-next-catalog</id>
                  <phase>prepare-package</phase>
                  <goals>
                     <goal>java</goal>
                  </goals>
                  <configuration>
                     <mainClass>io.openshift.booster.BoosterIndexer</mainClass>
                     <arguments>
                        <argument>${catalog.repository}</argument>
                        <argument>next</argument>
                        <argument>${project.build.directory}/booster-catalog-next</argument>
                        <argument>${project.build.outputDirectory}/booster-catalog-next.zip</argument>
                     </arguments>
                  </configuration>
               </execution>
            </executions>
         </plugin>
         <plugin>
            <artifactId>maven-release-plugin</artifactId>
            <configuration>
               <releaseProfiles>gpg-sign,jboss-release</releaseProfiles>
               <tagNameFormat>v@{project.version}</tagNameFormat>
            </configuration>
         </plugin>
         <plugin>
            <groupId>org.sonatype.plugins</groupId>
            <artifactId>nexus-staging-maven-plugin</artifactId>
            <version>1.6.8</version>
            <extensions>true</extensions>
            <configuration>
               <autoReleaseAfterClose>true</autoReleaseAfterClose>
               <nexusUrl>https://oss.sonatype.org/</nexusUrl>
               <serverId>ossrh</serverId>
            </configuration>
         </plugin>
      </plugins>
   </build>
   <properties>
      <snakeyaml.version>1.18</snakeyaml.version>
      <json.version>1.0.4</json.version>
      <jgit.version>4.8.0.201706111038-r</jgit.version>
      <!-- Indexer properties -->
      <catalog.repository>https://github.com/openshiftio/booster-catalog</catalog.repository>
   </properties>
   <dependencies>
      <!-- Compile-time dependency -->
      <dependency>
         <groupId>org.yaml</groupId>
         <artifactId>snakeyaml</artifactId>
         <version>${snakeyaml.version}</version>
      </dependency>
      <dependency>
         <groupId>org.glassfish</groupId>
         <artifactId>javax.json</artifactId>
         <version>${json.version}</version>
      </dependency>
      <dependency>
         <groupId>org.eclipse.jgit</groupId>
         <artifactId>org.eclipse.jgit</artifactId>
         <version>${jgit.version}</version>
      </dependency>
      <!-- Test dependencies -->
      <dependency>
         <groupId>junit</groupId>
         <artifactId>junit</artifactId>
         <version>4.8.2</version>
         <scope>test</scope>
      </dependency>
      <dependency>
         <groupId>org.assertj</groupId>
         <artifactId>assertj-core</artifactId>
         <version>3.6.2</version>
         <scope>test</scope>
      </dependency>
   </dependencies>
   <scm>
      <connection>scm:git:git://github.com/openshiftio/booster-catalog-service.git</connection>
      <developerConnection>scm:git:git@github.com:openshiftio/booster-catalog-service.git</developerConnection>
      <url>http://github.com/openshiftio/booster-catalog-service</url>
      <tag>HEAD</tag>
   </scm>
</project>
